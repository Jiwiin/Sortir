<ul class="nav justify-content-end fixed-top" >
    {% if is_granted('ROLE_ADMIN') %}
    <li class="nav-item">
        <a class="nav-link" href="{{ path('app_admin_index') }}" title="Administration">Administration</a>
    </li>
    {% endif %}
    <li class="nav-item">
        <a class="nav-link active" href="{{ path('app_event_index') }}" title="Accueil">Accueil</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" href="{{ path('app_user_show', {'id': app.user.id}) }}" title="Profil">Mon profil</a>
    </li>
    <li class="nav-item">
        <a class="nav-link" href="{{ path('app_logout') }}" title="">Se déconnecter ({{ app.user.username }})</a>
    </li>
</ul>
{% if app.request.requestUri == '/event/' %}
    <div class="container d-flex justify-content-end">
        <div class="d-flex flex-column align-items-end">
            <p>Date du jour : <strong>{{ "now"|format_datetime(pattern="d MMMM y", locale='fr') }}</strong></p>
            <p>Participant : <strong>{{ app.user.username }}</strong></p>
        </div>
    </div>
{% endif %}